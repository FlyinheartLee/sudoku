# 2026-03-23 记忆

## AgentTeamsBI 安装与配置
- 安装位置: ~/.openclaw/workspace/AgentTeamsBI/
- 端口: 7891
- 功能: 多Agent团队监控看板，实时显示Agent状态、Token用量日历、任务监督日志

## 已配置的5个Agent
1. 龙王 (main) - 中书省 - 任务协调
2. 参谋 (chat) - 门下省 - 深度分析  
3. 交易 (stock-analyst) - 尚书省 - 交易监控
4. 笔杆 (writer) - 尚书省 - 内容创作
5. 测试助手 (dmy) - 测试部 - 测试开发

## 修复的问题

### 1. 工作区路径分隔符修复
- 问题: server.py 使用 `workspace-{agent_id}` (连字符)，实际目录是 `workspace_{agent_id}` (下划线)
- 文件: dashboard/server.py
- 影响: API 找不到文件，详情页显示"暂无内容"

### 2. API正则表达式修复
- 问题: `(
+)` 无法匹配带连字符的agent_id (如 stock-analyst)
- 修复: 改为 `([\w-]+)` 支持连字符
- 文件: dashboard/server.py 第2923行

### 3. CSS滚动问题修复
- 修复文件: dashboard/dist/assets/index-aXD9ZGPG.css
- 修改: `.mp-body`, `.file-panel-body`, `.mp-file-content` 从 `overflow:hidden` 改为 `overflow:auto`
- 效果: 详情页可以正常滚动显示内容

### 4. 首页布局调整
- 修改: .agent-grid 从 `grid-template-columns:repeat(3,1fr)` 改为 `repeat(2,1fr)`
- 效果: 每行显示2个Agent卡片，文字显示更完整

### 5. server.py硬编码配置修复
- 修复了 `_DEFAULT_AGENT_DEPTS` 和 `agent_map` 中的agent IDs和名称
- 修复了 AGENTS.md 路径从 Windows 格式改为当前系统路径

## 自动同步
- 脚本: scripts/sync_agents.py
- 频率: 每60秒运行一次
- 功能: 检测workspace文件时间戳更新Agent状态

## 安装的技能
- gstack (Garry Tan的28个子技能)
- 位置: ~/.openclaw/skills/gstack/
- 已禁用遥测和更新检查

## 关键文件路径
- server.py: ~/.openclaw/workspace/AgentTeamsBI/dashboard/server.py
- CSS: ~/.openclaw/workspace/AgentTeamsBI/dashboard/dist/assets/index-aXD9ZGPG.css
- Agent数据: ~/.openclaw/workspace/AgentTeamsBI/data/agent_config.json
- Agent状态: ~/.openclaw/workspace/AgentTeamsBI/data/live_status.json
