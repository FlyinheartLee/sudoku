# 2026-03-18 记忆

## 飞书图片发送流程（已解决）
- ❌ 错误方法：只用 `read` 工具读取图片 → 返回图片数据但不会自动发送
- ✅ 正确方法：使用 `message` 工具指定 `media` 参数
  ```json
  { "action": "send", "media": "/path/to/image.png" }
  ```
- 图片固定目录：`/Users/flyinheart/.openclaw/workspace/`（已在 mediaLocalRoots 白名单中）

## Skill: xhs-cover
- 功能：生成小红书封面图片
- 原理：调用本地 ComfyUI API (Stable Diffusion) 生成 AI 图片
- 质量：照片级，远高于 PIL 代码绘图
- 配置：ComfyUI 本地地址 http://localhost:8188
- 状态：当前未运行，需启动后使用

## 图片生成方式对比
| 方式 | 质量 | 说明 |
|------|------|------|
| PIL 代码绘图 | ⭐⭐ | 简单几何图形、文字、渐变 |
| ComfyUI/Stable Diffusion | ⭐⭐⭐⭐⭐ | 照片级人物、场景 |
| DALL-E | ⭐⭐⭐⭐⭐ | 需 OpenAI API Key |
